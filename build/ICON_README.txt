# Place a Windows multi-resolution `icon.ico` here for the installer and exe.
# A generated icon from `public/images/logo-emblem.png` is committed as `icon.ico`.
# To replace it, convert your brand logo to `.ico` and overwrite `build/icon.ico`.
