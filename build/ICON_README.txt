# Windows packaging assets for Attendance Desktop (APPNEP)

- `icon.ico` — multi-resolution installer / exe icon generated from
  `public/images/logo-emblem.png` via `npm run build:icon`.

Regenerate:

```bash
npm run build:icon
```
